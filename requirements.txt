# ── Core ──────────────────────────────────
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# ── PostgreSQL ────────────────────────────
psycopg2-binary>=2.9
pgvector>=0.3          # ORM helper for pgvector column type

# ── Neo4j ────────────────────────────────
neo4j>=5.20

# ── Object Storage ───────────────────────
minio>=7.2

# ── Crawler ──────────────────────────────
scrapy>=2.11
playwright>=1.44        # install browsers separately: playwright install chromium
trafilatura>=1.10
readability-lxml>=0.8

# ── NLP / Extraction ─────────────────────
spacy>=3.7
simhash>=2.1           # segment-level deduplication

# ── Scheduling ───────────────────────────
# Uncomment one:
# apache-airflow>=2.9
# prefect>=2.19

# ── Utilities ────────────────────────────
pyyaml>=6.0
httpx>=0.27
tenacity>=8.3          # retry logic
structlog>=24.1        # structured logging

# ── Dev / Testing ─────────────────────────
pytest>=8.2
pytest-asyncio>=0.23